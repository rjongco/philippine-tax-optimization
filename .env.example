# Image tag docker compose pulls, or tags a local --build as. Any tag published
# to ghcr.io/rjongco/philippine-tax-optimization works.
TAG=latest

# Host port the app is served on. The container always listens on 8000.
PORT=8000
